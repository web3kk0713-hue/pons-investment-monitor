{
  "name": "pons-investment-monitor",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "collect": "node scripts/collect.mjs",
    "validate:data": "node scripts/validate-data.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/validate-data.mjs"
  }
}
